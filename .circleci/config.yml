version: 2
jobs:
  build:
    docker:
      - image: ubuntu:bionic
    working_directory: ~/repo

    steps:
      - checkout
      - run: apt-get update && apt-get install -y build-essential
      - run: make -j
